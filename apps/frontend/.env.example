# =================================================================
# 前端环境变量配置示例
# =================================================================
# 说明：
# - 复制此文件为 .env 并根据实际情况修改配置
# - 所有 VITE_ 前缀的变量会暴露给客户端代码
# - 修改配置后需要重启开发服务器才能生效
# - 被注释的配置项使用默认值，取消注释即可自定义
# =================================================================

# -----------------------------------------------------------------
# API 配置
# -----------------------------------------------------------------

# API 基础路径
# 开发环境使用相对路径，通过 Vite 代理转发到后端服务
# 生产环境可配置为绝对 URL（需包含后端前缀，例如 https://api.example.com/api）
VITE_API_BASE_URL=/api

# API 请求超时时间（毫秒）
# 超过此时间未响应的请求将被中断
# 默认值: 10000 (10秒)
VITE_API_TIMEOUT_MS=100000

# 流式连接超时时间（毫秒）
# 用于 SSE 和流式响应的初始连接超时
# 默认值: 30000 (30秒)
VITE_API_CONNECTION_TIMEOUT_MS=30000

# -----------------------------------------------------------------
# SSE (Server-Sent Events) 配置
# -----------------------------------------------------------------

# SSE 最大重试次数
# 连接断开后的最大自动重连次数
# 默认值: 10
VITE_SSE_MAX_RETRIES=10

# SSE 重试延迟基数（毫秒）
# 使用指数退避策略，此值为基础延迟时间
# 默认值: 1000 (1秒)
VITE_SSE_RETRY_DELAY_BASE=1000

# SSE 最大重连延迟（毫秒）
# 指数退避的上限，防止延迟过长
# 默认值: 30000 (30秒)
VITE_SSE_MAX_RECONNECT_DELAY=30000

# -----------------------------------------------------------------
# UI 配置
# -----------------------------------------------------------------

# SSE 事件历史最大记录数
# 超过此数量将自动裁剪旧记录
# 默认值: 1000
VITE_SSE_EVENT_HISTORY_MAX=1000

# SSE 事件历史裁剪后保留数量
# 当达到最大记录数时，保留最近的 N 条记录
# 默认值: 500
VITE_SSE_EVENT_HISTORY_SLICE=500

# 是否启用 SSE 自动重连
# 默认值: true
VITE_SSE_AUTO_RECONNECT=true

# -----------------------------------------------------------------
# Vite 开发/构建配置
# -----------------------------------------------------------------

# Dev Server API 代理目标（仅开发）
# 默认值: http://localhost:3000
VITE_DEV_API_PROXY_TARGET=http://localhost:3000

# 构建压缩阈值（字节）
# 小于此阈值的资源不进行 gzip/brotli 压缩
# 默认值: 10240 (10KB)
VITE_BUILD_COMPRESSION_THRESHOLD_BYTES=10240
